# リアルタイムチャット example — Sol への実装指示

## 現在の状態

- ユーザーの最新指示は「Sol は動かさず、指示出しだけ」。この計画を保存した段階で停止する。
- 再開の指示を受けてから Sol に実装を任せること。
- 先行して作成した `examples/realtime-chat/` と、ルート `README.md`・`pnpm-lock.yaml` の作業途中の変更がある。完成・検証済みとは扱わず、再開時に確認して引き継ぐ。他の変更を巻き戻さない。

## 目的と画面仕様

最近追加された `takibi/watch` を使い、リアルタイムの双方向チャットを1画面で体験できる example を `examples/realtime-chat/` に作る。

- デスクトップでは左右2ペイン。左は **Aさんがログインしている体**、右は **Bさんがログインしている体**にする。
- 実際の認証・認可は実装しない。表示名は Aさん／Bさんに固定し、ログインフォームや編集欄は不要。
- 各ペインは独立した watch client、購読、接続状態、入力欄、送信ボタン、下書き、エラー表示を持つ。
- Aさんの送信が両ペインへ届き、Bさんから返信できることを明確に見せる。各ペインでは自分の発言を右寄せ、相手の発言を左寄せにする。
- 共有ルーム選択で両ペインを同じルームへ切り替える。公開ルームは `lobby`、`help`、`random` 程度の固定リストでよい。
- 最新50件を時系列で表示する。狭い画面では縦並びにする。

## 実装方針

- 公開 SDK の `takibi` と `takibi/watch` を使う。SDK 本体の変更や新しい共通パッケージの分離は不要。
- Cloudflare Worker と SQLite Durable Object を使用し、公開ルームごとに名前付き Durable Object を1つ選ぶ。
- Worker は `/api/:room/*` のルームを検証してからルーティングする。未知のルームは拒否する。
- メッセージのスキーマは表示名と本文。本文は trim 後1〜500文字、表示名も長さを制限し、サーバー側で検証する。
- コレクションは定数の `fullAccess` とし、認証処理やユーザー別の権限判定は加えない。
- サーバー管理の `createdAt` と宣言済みインデックスで最新50件を降順に watch し、UIでは反転して時系列表示する。
- 送信は同じ client の HTTP `messages.add`、受信は `messages.watch`。ポーリングやペイン間の直接コピーで購読を代用しない。
- 軽量な TypeScript のブラウザ UI を Vite でビルドし、Wrangler が静的アセットと API を同一 origin で配信する。
- 接続中・接続済み・再接続中・終端状態を表示し、終端状態では再試行できるようにする。

## ライフサイクルと入力の注意点

- ルーム切替時は古い購読を解除し、古いメッセージ・件数表示をすぐ消す。ページ終了時も解除する。
- generation 等で古い購読と送信結果が新しいルームの UI を更新しないようにする。
- 送信中のルーム切替で、新しいルームの送信ボタンが無効のままにならないようリセットする。
- 送信失敗時は下書きを残す。送信待ちの間に入力された新しい下書きを、古い送信の成功で消さない。
- 多重送信を防ぎ、空白だけの本文を拒否する。
- ユーザーの本文と表示名は textContent 等で安全に描画する。

## ドキュメントと検証

- example README に起動コマンド、2ペインの試し方、構成、認証なしの公開デモであること、最新50件の表示制限を記す。
- `.wrangler` のローカル状態とビルド成果物をコミットしない。
- リポジトリの AGENTS.md に従う。実装検索は Semble を先に使う。Vite 設定は既存の vite-plus の規約に合わせる。
- ルーティング、入力検証、ルーム分離など実際の挙動を確認する適切なテストを用意し、型チェック・lint・build・関連テストを通す。
- 実装を再開した後は browser use で A→B、B→A の表示、ルーム切替と分離、再読込後の保存、空入力と HTML 風本文の扱いを確認する。
- 完了報告には起動方法と検証結果、未確認事項があればその内容を含める。現段階では実装・検証を再開しない。
